# Firmware binary (large, download from micropython.org)
ESP32_GENERIC_C2-*.bin

# Data logs
*.txt
# log.csv is tracked: weekly cloud routine reads it from GitHub for Notion updates (see sync_log.py)

# Arduino/PlatformIO build artifacts
.pio/
sketch/
src/

# platformio.ini kept for reference but builds won't work
